version: '3.6'

services:
  master-diet-app:
    image: master-diet-app:1
    build:
      context: ./
      dockerfile: Dockerfile
    volumes:
      - /data/master-diet-app-data
    ports:
      - "8081:8081"